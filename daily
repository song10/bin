#!/bin/sh
if [ '-r' = "$1" ]; then sed -i '$d' ~/.payrolltime; fi

payrolltime && exit

xalarm -q "Ready to Home, Rex." -a "8:50"
xalarm -q "Rex, Go Home Now!" -a "9:00"

if [ 2 = $? ]; then opt='-a -f'; fi

title
sudo echo -n # just to ask sudo password
aptup
#mygetup $opt
gitup
